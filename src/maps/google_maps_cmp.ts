import {OnInit, Component, EventEmitter, Input, Output, HostListener} from '@angular/core';

@Component({
    selector: 'google-maps-cmp',
    templateUrl: './google_maps.ng.html'
})
export class GoogleMapsCmp {
    constructor() {

    }
}
