import {OnInit, Component, EventEmitter, Input, Output, HostListener} from '@angular/core';

@Component({
    selector: 'accounts-cmp',
    templateUrl: './accounts.ng.html'
})
export class AccountsCmp {
    constructor() {

    }
}
